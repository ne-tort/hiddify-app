# CPU / synthetic benchmarks (not the integration stand)

- `collect_phase0_baseline.ps1` — PowerShell: прогон `go test` по `common/l3router`, сводка JSON в `bench/runtime/`.

Интеграционный стенд (VPS, Docker, SMB): [`../stand/l3router/README.md`](../stand/l3router/README.md).
