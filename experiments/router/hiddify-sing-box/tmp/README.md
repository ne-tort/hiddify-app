# Deprecated path

The l3router integration stand moved to:

`experiments/router/stand/l3router/`

Use `python run.py` there (see that directory’s README). This `tmp/` folder is not the source of truth.
