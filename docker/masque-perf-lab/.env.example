# Copy to .env next to docker-compose.remote.yml (Compose substitutes ${VAR:-default}).
# See README.md and AGENTS.md §15.

# HIDDIFY_MASQUE_CONNECT_IP_DEBUG=1
# MASQUE_QUIC_PACKET_CONN_POLICY=strict
