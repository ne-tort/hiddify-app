# MASQUE / CONNECT-IP — риски деградации и пробелы (код + стенд)

Цель: единый список **наблюдаемых** и **потенциальных** причин потерь/таймаутов на Docker-стенде и в production-подобной нагрузке. Часть пунктов подтверждена прогонами `degrade_matrix` и triage по boundary; часть — гипотезы по коду и апстриму, требующие счётчиков/peer-split.

## 0) Статус мер (обновляемый)

- **[DONE] CONNECT-IP payload hard-cap (ядро, не раннер):** в `transport/masque/transport.go` введён и применён `connectIPUDPWriteHardCap=1152` для `initialPayload` и `maxUDPPayload` в `newConnectIPUDPPacketConn`. Это компенсирующая мера против oversize/MTU-ошибок на TX boundary при high-rate.
- **[DONE] CONNECT-IP local MTU retry (ядро, не раннер):** в `connectIPUDPPacketConn.WriteTo` добавлен ограниченный локальный retry-path на `DatagramTooLarge/EMSGSIZE` с `decreasePayloadCeiling("local_mtu_error")` и повторной отправкой уменьшенного chunk (до 3 попыток). Это снижает вероятность потерь при transient oversize на high-rate.
- **[DONE] QUIC PacketConn contract hardening (ядро):** добавлены `ValidateQUICTransportPacketConn` (TierA/TierB + policy strict/permissive), server-side fail-fast в `endpoint_server.go` до `http3.Server.Serve`, custom `QUICDial` policy gate в client paths (`connect_udp/connect_ip/connect_stream`) с явным degraded logging и observability полями `quic_transport_packet_conn_tier|type|buffer_tuning_*` в CONNECT-IP snapshot. Runtime evidence на стенде: warning теперь структурирован с `conn_type=*masque.connectedPacketConn` (client custom dial path).
- **[DONE] HTTP/3 stream DATAGRAM очередь: headroom + структура (ядро, не раннер):** дефолт per-stream очереди `4096`, env `HIDDIFY_HTTP3_STREAM_DATAGRAM_QUEUE_LEN` (`128..65536`). Дополнительно очередь реализована как **фиксированное кольцо** (O(1) dequeue), без сдвига слайса на каждом `ReceiveDatagram` — снижение CPU/GC на bulk CONNECT-IP/CONNECT-UDP.
- **[DONE] Unknown-stream DATAGRAM race buffering (ядро, не раннер):** в `replace/quic-go-patched/http3/conn.go` добавлен bounded pending-buffer для DATAGRAM, пришедших до `TrackStream` (пер-stream `64`, global `4096`). Это снижает потери по setup race и уменьшает `unknownStreamDatagramDropTotal` на high-rate старте.
- **[DONE] Unknown-stream pending backlog anti-stall (ядро, не раннер):** в `replace/quic-go-patched/http3/conn.go` добавлен bounded eviction (`drop-oldest`) для per-stream/global pending очередей + очистка pending при lifecycle (`clearStream`). Это компенсирующая мера против залипания старых unknown stream-id и последующих ложных потерь новых DATAGRAM при high-rate.
- **[DONE] QUIC conn-level DATAGRAM receive queue без «вечного» cap слайса (ядро):** в `replace/quic-go-patched/datagram_queue.go` приёмная очередь переведена на **фиксированное кольцо** `maxDatagramRcvQueueLen` (ленивая аллокация при первом кадре): после burst больше нет сохранённого огромного `cap` у `[][]byte`, что снижает baseline память/давление на GC между пиками PPS (`hiddify-core` `6129f004`).
- **[DONE] QUIC packet-packer ACK-vs-DATAGRAM budget arbitration (ядро, hot path):** в `replace/quic-go-patched/packet_packer.go` добавлен приоритет DATAGRAM при конфликте бюджета с ACK (`DATAGRAM` влезает без ACK, но не влезает вместе с ACK). Это убирает повторный defer головы очереди DATAGRAM под ACK-pressure и снижает риск TX-side no-progress без дополнительного drop. (Коммит `hiddify-core` `32724914` закрепил поведение + обновление `packet_packer_test`.)
- **[DONE] Read-side lock narrowing (ядро):** в `replace/quic-go-patched/http3/conn.go` реестр stream → **`RWMutex`**, горячий `receiveDatagrams` только **`RLock`**. В vendored `third_party/connect-ip-go/conn.go` снимки policy для каждого пакета под **`RLock`**. Цель: меньше контенции между приёмником DATAGRAM и редкими мутациями карты / маршрутов.
- **[DONE] CONNECT-IP OBS periodic_active sampling (ядро):** в `transport/masque/transport.go` после первого успешного epoch горячие вызовы `maybeEmitConnectIPActiveSnapshot` сэмплируются (**~1 из 128**): меньше `time.Now`/CAS на каждую датаграмму при неизменной частоте JSON-OBS (~1 Hz). Коммит `hiddify-core` `ac74034`.
- **[DONE] CONNECT-IP OBS без лишнего атомика сэмплера (ядро):** `maybeEmitConnectIPActiveSnapshot` использует модуль от **уже инкрементированных** `packetRxTotal`/`packetTxTotal` вместо отдельного `connectIPActiveObsSampleCounter.Add(1)` на каждом кадре после первого emit. Коммит `hiddify-core` `70910300`.
- **[DONE] HTTP/3 per-stream DATAGRAM backlog (ядро):** дефолт очереди `state_tracking_stream` с **4096 → 8192** (`replace/quic-go-patched/http3/state_tracking_stream.go`, коммит `hiddify-core` `2508f5a0`) до отведения приложением `ReceiveDatagram`; ENV `HIDDIFY_HTTP3_STREAM_DATAGRAM_QUEUE_LEN` без изменений. Стендовое подтверждение `last_pass`/счётчиков — после рабочего compose (локально блокировался pull/buildx).
- **[DONE] CONNECT-IP policy path без RLock на пакет (vendor):** в `third_party/connect-ip-go/conn.go` политика `assignedAddresses` / `localRoutes` / `peerAddresses` для **каждого** RX/TX datagram читается из **atomic snapshot** (`connRouteView`), публикуемого только при редких обновлениях маршрутов; **`sync.RWMutex` заменён на `Mutex`**, hot path не берёт lock. Снижает контенцию под high PPS. Коммит `hiddify-core` `f78b8248ede99608c009742e474ef4390c795468`. Плюс **`ReadPacket`**: ошибка при коротком буфере вместо усечения.
- **[DONE] CONNECT-IP policy validation loop de-closure (vendor, hot path):** в `third_party/connect-ip-go/conn.go` RX/TX policy checks переведены с `slices.ContainsFunc`/замыканий на прямые циклы (`prefixesContainAddr*`, `routesAllow*`) без изменения allow/deny семантики. Цель: уменьшить CPU overhead per packet на границе high PPS.
- **[DONE] CONNECT-IP policy route-loop family/ICMP fast-path (vendor, hot path):** в `third_party/connect-ip-go/conn.go` для `routesAllowDestinationAndProtocol` / `routesAllowSourceAndProtocolSameFamily` / `routeAllowsDestinationButNotProtocol` добавлены ранние фильтры по IP family и вынесен `isICMPProtocol` из тела циклов. Это убирает повторную работу на каждом route-элементе и сокращает CPU на packet-plane при высокой скорости.
- **[DONE] CONNECT-IP datagram context-id zero fast-path (vendor, hot path):** в `third_party/connect-ip-go/conn.go` в `ReadPacket` добавлен `parseDatagramContextID` с быстрым путём для доминирующего случая `Context ID=0` (`0x00`) без `quicvarint.Parse` на каждый пакет. Локальный microbench в `conn_test.go`: `context_zero_fast_path=1.162 ns/op` vs `context_non_zero_varint=1.874 ns/op`, 0 alloc/op; unit-тесты на zero/non-zero/empty сохранены.
- **[DONE] CONNECT-IP datagram context-id single-byte non-zero fast-path (vendor, hot path):** в `third_party/connect-ip-go/conn.go` `parseDatagramContextID` теперь обрабатывает одно-байтовые non-zero context-id (`00xxxxxx`, диапазон `1..63`) без `quicvarint.Parse`; это снижает overhead tolerant-drop path при noisy ingress/high PPS без изменения RFC-семантики (non-zero context по-прежнему drop). Локально: `go test -count=1 ./... -run 'TestParseDatagramContextID|TestAdaptivePrefetchProbeGate|TestReadPacketWithContextDeadlineExceeded|TestReadPacketWithContextReturnsPrefetchedAfterDeadline|TestValidateIncomingProxiedPacketBypassesWhenPolicyDisabled|TestIncomingDatagrams|TestSendingDatagrams|TestRouteContainingAddrBinarySearch' -bench 'BenchmarkParseDatagramContextID|BenchmarkValidateIncomingProxiedPacket|BenchmarkRoutePolicyLookups|BenchmarkTakePrefetchedRaw' -benchmem` PASS; `context_non_zero_single_byte=1.134 ns/op`, 0 alloc/op.
- **[DONE] CONNECT-IP/CONNECT-UDP datagram context-id multi-byte non-zero fast-reject (vendor, hot path):** в `third_party/connect-ip-go/conn.go` и `third_party/masque-go/conn.go` добавлен ранний reject для multi-byte varint context-id, когда старшие value-биты уже ненулевые (`data[0]&0x3f != 0`), без вызова `quicvarint.Parse`. Это убирает лишний parse overhead на tolerant-drop path под noisy/high-rate ingress, сохраняя семантику поддержки только context-id=0. Локально: `connect-ip-go`/`masque-go` targeted `go test`+bench PASS; новые бенчи `context_non_zero_two_byte_highbits`: `connect-ip=1.143 ns/op`, `masque-go=1.329 ns/op`, 0 alloc/op.
- **[DONE] CONNECT-IP/CONNECT-UDP datagram context-id multi-byte zero + lowbits non-zero fast-path (vendor, hot path):** в `third_party/connect-ip-go/conn.go` (`parseDatagramContextID`) и `third_party/masque-go/conn.go` (`parseProxiedDatagramPayload`) добавлен быстрый разбор canonical multi-byte `context-id=0` (`2/4/8` bytes) и быстрый reject для ветки `data[0]&0x3f==0` с ненулевым tail byte без `quicvarint.Parse`. Это закрывает оставшийся varint parse overhead в tolerant-drop/read path при high PPS noisy ingress. Локально: targeted unit+bench PASS; новые бенчи `context_zero_two_byte`: `connect-ip=1.521 ns/op`, `masque-go=1.694 ns/op`, 0 alloc/op; `hiddify-sing-box` `go test -count=1 ./transport/masque/... ./protocol/masque/...` PASS.
- **[DONE] CONNECT-IP/CONNECT-UDP strict fast parser-path cleanup (vendor, hot path):** в `third_party/connect-ip-go/conn.go` (`parseDatagramContextID`) и `third_party/masque-go/conn.go` (`parseProxiedDatagramPayload`) удалён недостижимый fallback на `quicvarint.Parse` после полного покрытия inline ветками (`0/1/2/4/8-byte` zero + non-zero reject). Это фиксирует parser boundary как strictly fast-path и убирает лишнюю generic-parse ветку из read hot loop без изменения tolerant-drop/RFC семантики. Локально: targeted `connect-ip-go` tests+bench PASS (`context_zero_fast_path=0.939 ns/op`, `context_non_zero_varint=0.938 ns/op`, `context_zero_two_byte=1.133 ns/op`, 0 alloc/op), targeted `masque-go` tests+bench PASS (`context_zero_fast_path=1.110 ns/op`, `context_non_zero_varint=0.930 ns/op`, `context_zero_two_byte=1.295 ns/op`, 0 alloc/op), `hiddify-sing-box ./transport/masque/... ./protocol/masque/...` PASS.
- **[DONE] CONNECT-UDP server ingress context-id=0 compatibility hardening (vendor, resilience):** в `third_party/masque-go/proxy.go` `extractContextZeroPayloadForUDP` больше не ограничен strict `0x00`: parser-path переведён на `parseProxiedDatagramPayload`, поэтому canonical multi-byte varint `context-id=0` (`2/4/8` bytes) принимаются в server ingress без регрессии tolerant-drop для non-zero/malformed datagram. Добавлен `TestExtractContextZeroPayloadForUDP` (single-byte zero accept, two-byte zero accept, non-zero reject). Локально: `third_party/masque-go` targeted `go test` + bench PASS (`context_zero_fast_path=1.416 ns/op`, `context_zero_two_byte=1.697 ns/op`, 0 alloc/op); smoke `udp`/`tcp_ip` (10 MiB @ 25_000_000 B/s) PASS, hash/loss regressions не наблюдаются.
- **[DONE] CONNECT-UDP server ingress transient UDP send-pressure hardening (vendor, resilience):** в `third_party/masque-go/proxy.go` fallback path записи в UDP (`writePayloadsFallback`) больше не fail-closed на кратковременных ошибках сокета (`ENOBUFS`/`EAGAIN`/`EWOULDBLOCK`/`EINTR`/timeout): payload sampled-drop и batch продолжает drain, чтобы не рвать CONNECT-UDP поток из-за временного backpressure ядра. Добавлен `TestIsTransientUDPSendError`; локально targeted `third_party/masque-go` + cross-check `third_party/connect-ip-go` + `hiddify-sing-box ./transport/masque/... ./protocol/masque/...` PASS.
- **[DONE] CONNECT-UDP server egress transient UDP read-pressure hardening + sampled oversize logging (vendor, resilience):** в `third_party/masque-go/proxy.go` путь `proxyConnReceive` больше не fail-closed на кратковременных read ошибках UDP (`ENOBUFS`/`EAGAIN`/`EWOULDBLOCK`/`EINTR`/`ECONNREFUSED`/`ECONNRESET`/timeout): пакет tolerant-drop и цикл продолжается, чтобы не рвать CONNECT-UDP поток на transient pressure/ICMP noise. Параллельно oversize-drop логирование на receive-path переведено на sampled режим (`1` + каждый `1024`-й), чтобы убрать log-storm backpressure в high-rate noisy ingress. Добавлен `TestIsTransientUDPReadError`; локально targeted `third_party/masque-go` + `hiddify-sing-box ./transport/masque/... ./protocol/masque/...` PASS.
- **[DONE] CONNECT-UDP server egress `DatagramTooLarge` tolerant-drop hardening (vendor, resilience):** в `third_party/masque-go/proxy.go` путь `proxyConnReceive` теперь отдельно классифицирует `*quic.DatagramTooLargeError` от `str.SendDatagram` и не рвёт CONNECT-UDP сессию: oversized к QUIC payload sampled-drop, loop продолжает forwarding. Добавлен `TestIsHTTPDatagramTooLargeError`; локально targeted `third_party/masque-go` + cross-check `third_party/connect-ip-go` + `hiddify-sing-box ./transport/masque/... ./protocol/masque/...` PASS. Smoke (Windows Docker, `10 MiB @ 25_000_000 B/s`): `udp` PASS (`stop_reason=none`, `payload_hash_ok=true`, `measured_loss_pct_approx=0`, `throughput_mbps=135.199`), `tcp_ip` PASS (`hash_ok=true`, `loss_pct=0`, `throughput_mbps=36.794`, HTTP3/QUIC drop deltas=0).
- **[DONE] CONNECT-UDP transient-pressure anti-spin micro-backoff (vendor, resilience under sustained socket pressure):** в `third_party/masque-go/proxy.go` добавлен bounded adaptive backoff (`50us .. 3.2ms`) для подряд идущих transient ошибок в `writePayloadsFallback` (UDP send) и `proxyConnReceive` (UDP read / HTTP DATAGRAM send). На единичных transient ошибках поведение прежнее (drop+continue без сна), при серии ошибок убирается busy-spin/self-inflicted CPU thrash до появления прогресса. Добавлены тесты `TestTransientBackoffDuration` и `TestTransientPressureBackoffReset`; локально targeted `third_party/masque-go` + cross-check `third_party/connect-ip-go` + `hiddify-sing-box ./transport/masque/... ./protocol/masque/...` PASS.
- **[DONE] CONNECT-UDP fallback backoff continuity across batch boundaries (vendor, resilience under sustained pressure):** в `third_party/masque-go/proxy.go` состояние backoff в `udpDatagramWriter.writePayloadsFallback` переведено из локальной переменной в поле writer (`sendBackoff`), чтобы серия transient UDP send ошибок не сбрасывала backoff на каждом новом batch и не возвращала batch-level busy-spin под длительным socket-pressure. Добавлен регрессионный `TestTransientPressureBackoffPersistsAcrossBatches`; локально targeted `third_party/masque-go` + cross-check `third_party/connect-ip-go` + `hiddify-sing-box ./transport/masque/... ./protocol/masque/...` PASS.
- **[DONE] CONNECT-UDP writer backoff cap/reset hardening against self-throttle (vendor, resilience + throughput):** в `third_party/masque-go/proxy.go` уменьшен верхний cap transient micro-backoff (`3.2ms -> 0.8ms`) и добавлен `sendBackoff.onProgress()` на успешном/частично успешном batched send (`WriteBatch`) в `udpDatagramWriter`, чтобы stale high-backoff не тянулся в fallback после реального прогресса. Это снижает риск self-inflicted throughput collapse (`receiver_incomplete`) при burst pressure без возврата busy-spin. Обновлён `TestTransientBackoffDuration` (новый cap), targeted `masque-go`/`connect-ip-go`/`transport+protocol masque` тесты PASS; Win smoke `udp` (10 MiB @ 25_000_000 B/s) PASS (`throughput_mbps=144.989`, `loss=0`, `payload_hash_ok=true`).
- **[DONE] CONNECT-IP policy-drop ICMP fail-logging sampling (vendor, resilience under noisy ingress):** в `third_party/connect-ip-go/conn.go` fail-path в `emitPolicyDropICMP` (`failed to compose/send policy-drop ICMP`) переведён с per-packet `log.Printf` на sampled logging (`1` + каждый `1024`-й) через отдельные счётчики (`policyDropICMPComposeFail`, `policyDropICMPSendFail`). Это убирает log-storm/self-inflicted I/O+CPU pressure при high-rate policy-drop burst без изменения контракта policy-drop ICMP.
- **[DONE] CONNECT-IP policy-drop ICMP fast-skip/precheck hardening (vendor, hot path resilience):** в `third_party/connect-ip-go/conn.go` `emitPolicyDropICMP` больше не делает попытку emission для `src_not_allowed` (только reason accounting), а для остальных причин выполняет pre-check через `validateOutgoingProxiedPacket` до входа в `WritePacket` compose/send path. Это убирает заведомо бесполезные попытки ICMP-send под policy deny и снижает self-inflicted pressure в policy-drop storm. Добавлен `TestEmitPolicyDropICMPSkipsSrcNotAllowedWithoutAttempt`; локально targeted `connect-ip-go` tests+bench PASS (`policy_enabled_validate=13.55 ns/op`, `take_prefetched_empty=1.694 ns/op`, 0 alloc/op), cross-check `third_party/masque-go` + `hiddify-sing-box ./transport/masque/... ./protocol/masque/...` PASS.
- **[DONE] CONNECT-UDP server ingress transient UDP send classification расширен (`ECONNREFUSED`/`ECONNRESET`) (vendor, resilience):** в `third_party/masque-go/proxy.go` `isTransientUDPSendError` теперь помечает `ECONNREFUSED` и `ECONNRESET` как transient на send-path. Это закрывает fail-closed сценарий, где кратковременный ICMP/socket-pressure шум на connected UDP мог разрывать CONNECT-UDP ingest loop вместо tolerant-drop + продолжения drain. Добавлены тесты в `conn_internal_test.go` (`TestIsTransientUDPSendError`), локально targeted `third_party/masque-go` + cross-check `third_party/connect-ip-go` и `hiddify-sing-box ./transport/masque/... ./protocol/masque/...` — PASS.
- **[DONE] CONNECT-UDP server ingress forced try-drain recovery on transient `ReceiveDatagram` (vendor, resilience + throughput under pressure):** в `third_party/masque-go/proxy.go` (`proxyConnSend`) transient ошибка blocking `ReceiveDatagram` теперь сначала запускает bounded forced drain через `TryReceiveDatagram` (до `proxiedConnPrefetchMax`) и сразу flush в UDP batch writer; sampled-drop/backoff используется только если drain не дал прогресса. Это снижает риск self-throttle (`receiver_incomplete`) при кратковременном receive-pressure, когда backlog уже доступен в try-drain path. Локально: `third_party/masque-go` targeted `go test` + bench PASS (`context_zero_fast_path=1.424 ns/op`, `context_non_zero_varint=1.325 ns/op`, `take_prefetched_empty=1.698 ns/op`, 0 alloc/op), `third_party/masque-go go test -race -count=1 ./...` PASS; cross-check `third_party/connect-ip-go` + `hiddify-sing-box ./transport/masque/... ./protocol/masque/...` PASS.
- **[DONE] CONNECT-UDP server ingress drop-only storm hardening (vendor, resilience under noisy ingress):** в `third_party/masque-go/proxy.go` (`proxyConnSend`) добавлена классификация extract-path (`malformed` / `unknown context` / `oversize` / `forwardable`) + sampled counters/logging и bounded micro-backoff для сценария, когда blocking receive + bounded try-drain не дают ни одного forwardable payload (pure drop backlog). Также `adaptiveTryDrainGate.observeDrain` теперь учитывает только число forwardable datagram, чтобы backoff probe не сбрасывался на drop-only ingress. Это снижает риск CPU thrash/self-throttle при high-rate шуме без изменения tolerant-drop контрактов. Локально: targeted `third_party/masque-go go test`+bench PASS (`context_zero_fast_path=1.322 ns/op`, `context_non_zero_varint=1.323 ns/op`, `take_prefetched_empty=1.693 ns/op`, 0 alloc/op), `third_party/masque-go go test -race -count=1 ./...` PASS; cross-check `third_party/connect-ip-go` + `hiddify-sing-box ./transport/masque/... ./protocol/masque/...` PASS.
- **[DONE] CONNECT-UDP server ingress skip-budget starvation fix after drop-first datagram (vendor, resilience + throughput):** в `third_party/masque-go/proxy.go` (`proxyConnSend`) после non-forwardable blocking `ReceiveDatagram` (malformed / unknown context / oversize) добавлен **forced bounded try-drain** в обход `adaptiveTryDrainGate` skip-budget. Это закрывает gap, когда queued valid context-0 payload мог задерживаться за drop-first кадром из-за stale skip-budget и приводить к self-throttle/throughput collapse под burst. Добавлен регрессионный `TestProxyConnSendForcesTryDrainAfterDropWhenGateSkips`; локально: targeted `third_party/masque-go` go test+bench PASS (`context_zero_fast_path=1.377 ns/op`, `context_non_zero_varint=1.330 ns/op`, `take_prefetched_empty=1.703 ns/op`, 0 alloc/op), cross-check `third_party/connect-ip-go` + `hiddify-sing-box ./transport/masque/... ./protocol/masque/...` PASS.
- **[DONE] CONNECT-UDP ingress hot-loop allocation/log-pressure hardening (vendor, resilience + throughput):** в `third_party/masque-go/proxy.go` server path `proxyConnSend` больше не создаёт заново `payloadBatch` на каждую итерацию read-loop (буфер вынесен на scope функции), а `extractContextZeroPayloadForUDP` получил inline fast-path для `context-id=0`/single-byte non-zero reject без лишнего parse-path в доминирующих кейсах. Дополнительно drop-only noisy ingress переведён на counter-only для `unknown context`/`malformed` (без per-sample log I/O), чтобы не создавать self-inflicted log backpressure при high PPS storm. Локально: `third_party/masque-go` targeted `go test`+bench PASS (`extract context_zero_fast_path=1.484 ns/op`, `extract non_zero_single_byte_fast_reject=1.673 ns/op`, `take_prefetched_empty=1.673 ns/op`, 0 alloc/op), cross-check `third_party/connect-ip-go` + `hiddify-sing-box ./transport/masque/... ./protocol/masque/...` PASS.
- **[DONE] CONNECT-UDP ingress drop-accounting contention hardening (vendor, resilience + throughput under drop-storm):** в `third_party/masque-go/proxy.go` `extractContextZeroPayloadForUDP` больше не делает per-packet atomic side-effects для `unknown context`/`malformed`/`oversize`; функция только классифицирует datagram, а `proxyConnSend` агрегирует drop-результаты и flush-ит счётчики батчем в конце итерации (`flushUDPPayloadDropTally`). Это убирает лишний atomic contention на noisy high-rate ingress без изменения tolerant-drop/RFC semantics. Локально: `third_party/masque-go` targeted `go test`+bench PASS (`extract_context_zero_fast_path=1.479 ns/op`, `extract_non_zero_single_byte_fast_reject=1.558 ns/op`, `extract_context_zero_two_byte=1.675 ns/op`, `extract_context_zero_four_byte=1.948 ns/op`, `take_prefetched_empty=1.673 ns/op`, 0 alloc/op); cross-check `third_party/connect-ip-go` (`policy_enabled_validate=14.18 ns/op`) и `hiddify-sing-box ./transport/masque/... ./protocol/masque/...` PASS.
- **[DONE] CONNECT-UDP fallback partial-progress accounting hardening (vendor, resilience + integrity under burst pressure):** в `third_party/masque-go/proxy.go` (`udpDatagramWriter.writePayloadsFallback`) `successfulWritesInBatch` теперь стартует со значения `start` (число уже отправленных payload после partial `WriteBatch`). Это устраняет ложный `no progress` в tail-drop guard и не даёт агрессивно дропать остаток batch после частичного восстановления сокета, снижая риск self-inflicted hash drift на transient pressure. Локально: `third_party/masque-go` targeted `go test`+bench PASS (`context_zero_fast_path=1.444 ns/op`, `context_non_zero_varint=1.340 ns/op`, `extract_context_zero_fast_path=1.518 ns/op`, `take_prefetched_empty=1.708 ns/op`, 0 alloc/op); cross-check `third_party/connect-ip-go` (`policy_enabled_validate=14.18 ns/op`) и `hiddify-sing-box ./transport/masque/... ./protocol/masque/...` PASS.
- **[DONE] CONNECT-UDP batched-send unsupported fallback duplicate-send fix (vendor, resilience + integrity under mixed kernel paths):** в `third_party/masque-go/proxy.go` (`udpDatagramWriter.writePayloadBatch`) ветка `isBatchUnsupported(err)` больше не делает fallback с `start=0` после partial `WriteBatch`: теперь учитывается фактический `sent` (`observeBatchProgress(sent)` + `writePayloadsFallback(..., sent)`). Это устраняет риск повторной отправки уже доставленных payload и hash drift при редком сочетании partial-progress и unsupported batch path. Локально: `third_party/masque-go` targeted tests+bench PASS (`context_zero_fast_path=1.450 ns/op`, `context_non_zero_varint=1.295 ns/op`, `take_prefetched_empty=1.663 ns/op`, 0 alloc/op), cross-check `third_party/connect-ip-go` (`policy_enabled_validate=13.55 ns/op`) и `hiddify-sing-box ./transport/masque/... ./protocol/masque/...` PASS.
- **[DONE] CONNECT-UDP fallback bounded retry hardening под transient send-pressure (vendor, resilience + integrity):** в `third_party/masque-go/proxy.go` (`udpDatagramWriter.writePayloadsFallback`) перед окончательным tolerant-drop добавлен ограниченный retry-path: не более одного retry после backoff внутри fallback-batch и только до первого успешного write в этом batch (`shouldRetryTransientFallback`). Это даёт сокету короткое recovery-окно под burst transient pressure без возврата retry-storm/busy-spin. Добавлен `TestShouldRetryTransientFallback`; локально: `third_party/masque-go` targeted `go test`+bench PASS (`context_zero_fast_path=1.331 ns/op`, `context_non_zero_varint=1.328 ns/op`, `extract_context_zero_fast_path=1.511 ns/op`, `take_prefetched_empty=1.696 ns/op`, 0 alloc/op), cross-check `third_party/connect-ip-go` (`policy_enabled_validate=14.21 ns/op`) и `hiddify-sing-box ./transport/masque/... ./protocol/masque/...` PASS.
- **[DONE] CONNECT-UDP fallback retry gate refinement после partial-progress drops (vendor, resilience + integrity):** в `third_party/masque-go/proxy.go` `shouldRetryTransientFallback` отвязан от `successfulWritesInBatch`: retry остаётся строго bounded (один retry после одного bounded pause на fallback-batch), но теперь не отключается после частичного прогресса и не переводит редкие transient send blips в безусловный payload drop (`receiver_incomplete`/hash drift). Обновлён `TestShouldRetryTransientFallback`; локально: targeted `third_party/masque-go` tests+bench PASS (`context_zero_fast_path=1.309 ns/op`, `context_non_zero_varint=1.301 ns/op`, `extract_context_zero_fast_path=1.485 ns/op`, `take_prefetched_empty=1.666 ns/op`, 0 alloc/op), `hiddify-sing-box ./transport/masque/... ./protocol/masque/...` PASS. Smoke (Windows Docker, `10 MiB @ 25_000_000 B/s`): `udp` PASS (`stop_reason=none`, `throughput_mbps=139.94`, `payload_hash_ok=true`, `loss≈0`), `tcp_ip` PASS (`hash_ok=true`, `loss_pct=0`, `throughput_mbps=37.06`, HTTP3/QUIC drop deltas=0).
- **[DONE] CONNECT-UDP fallback retry gate progress-aware re-hardening (vendor, resilience + hot-path stability):** в `third_party/masque-go/proxy.go` `shouldRetryTransientFallback` снова ограничивает retry только pre-progress фазой fallback-batch (`successfulWritesInBatch==0`) при сохранении bounded budget (`transientFallbackRetryMaxPerBatch=1`) и раннего retry в `backoff=0` кейсе. Это убирает post-progress retry syscall churn и снижает риск self-throttle под sustained high PPS transient send-pressure. `conn_internal_test.go::TestShouldRetryTransientFallback` дополнен кейсом `successfulWritesInBatch>0 -> retry denied`. Локально: targeted `third_party/masque-go` tests+bench PASS (`context_zero_fast_path=1.332 ns/op`, `context_non_zero_varint=1.325 ns/op`, `extract_context_zero_fast_path=1.510 ns/op`, `take_prefetched_empty=1.697 ns/op`, 0 alloc/op), cross-check `third_party/connect-ip-go` (`policy_enabled_validate=13.97 ns/op`) и `hiddify-sing-box ./transport/masque/... ./protocol/masque/...` PASS.
- **[DONE] CONNECT-UDP fallback pause-on-pressure hardening under mixed-success batch (vendor, resilience + throughput):** в `third_party/masque-go/proxy.go` (`udpDatagramWriter.writePayloadsFallback`) добавлен отдельный pause-gate `shouldPauseTransientFallback`: один bounded sleep на fallback-batch выполняется даже когда retry-гейт уже закрыт из-за частичного прогресса (`successfulWritesInBatch>0`). Это убирает tight drop/write loop и снижает риск syscall storm/self-throttle на transient `ENOBUFS/EAGAIN` после частичного восстановления сокета без возврата retry-storm. Добавлен `TestShouldPauseTransientFallback`; локально: targeted `third_party/masque-go` `go test`+bench PASS (первый запуск дал известный flaky `TestReadDeadline/extending_the_deadline`, повтор PASS: `context_zero_fast_path=1.297 ns/op`, `context_non_zero_varint=1.295 ns/op`, `extract_context_zero_fast_path=1.477 ns/op`, `take_prefetched_empty=1.666 ns/op`, 0 alloc/op), cross-check `third_party/connect-ip-go` (`policy_enabled_validate=14.46 ns/op`) и `hiddify-sing-box ./transport/masque/... ./protocol/masque/...` PASS.
- **[DONE] CONNECT-UDP fallback one-sleep-per-batch transient hardening (vendor, resilience + throughput):** в `third_party/masque-go/proxy.go` (`udpDatagramWriter.writePayloadsFallback`) ограничен sleep на transient UDP send-pressure: не более одного backoff-sleep на fallback batch при сохранении `sendBackoff` streak/cap/reset между batch-вызовами. Это снимает риск self-inflicted throughput collapse из-за per-payload sleep storm под burst socket pressure, не меняя tolerant-drop семантику для transient ошибок. Добавлен `TestShouldSleepOnTransientFallback`; локально targeted `third_party/masque-go` tests+bench PASS (`context_zero_fast_path=1.328 ns/op`, `extract_context_zero_fast_path=1.516 ns/op`, `take_prefetched_empty=1.707 ns/op`, 0 alloc/op), cross-check `third_party/connect-ip-go` + `hiddify-sing-box ./transport/masque/... ./protocol/masque/...` PASS.
- **[DONE] CONNECT-UDP fallback sustained-pressure tail-drop guard (vendor, resilience + throughput under ENOBUFS/EAGAIN storm):** в `third_party/masque-go/proxy.go` (`udpDatagramWriter.writePayloadsFallback`) добавлен bounded early-abort fallback batch: после достигнутого one-sleep-per-batch и backoff `>=400us` остаток payload текущего batch дропается без дополнительных `conn.Write`, с учётом `transientUDPSendTailDropTotal`. Это убирает syscall storm/self-throttle при длительном socket-pressure, сохраняя best-effort/tolerant-drop контракт. Добавлен `TestShouldDropTransientFallbackTail`; локально targeted `third_party/masque-go` tests+bench PASS (`context_zero_fast_path=1.427 ns/op`, `context_non_zero_varint=1.325 ns/op`, `take_prefetched_empty=1.705 ns/op`, 0 alloc/op), cross-check `third_party/connect-ip-go` + `hiddify-sing-box ./transport/masque/... ./protocol/masque/...` PASS.
- **[DONE] CONNECT-UDP fallback tail-drop anti-overdrop refinement (vendor, resilience + integrity under mixed pressure):** в `third_party/masque-go/proxy.go` (`udpDatagramWriter.writePayloadsFallback`) tail-drop guard усилен условием `remaining>=8`: bounded early-abort включается только для действительно больших fallback-batch при sustained pressure (`backoff>=400us` + one-sleep-per-batch), а короткие batch продолжают best-effort write attempts. Это снижает риск self-inflicted `receiver_incomplete`/hash drift на умеренном transient pressure без возврата syscall storm. `conn_internal_test.go::TestShouldDropTransientFallbackTail` расширен под новый контракт (`remaining` negative/positive); локально targeted `third_party/masque-go` tests+bench PASS (`context_zero_fast_path=1.429 ns/op`, `context_non_zero_varint=1.325 ns/op`, `extract_context_zero_fast_path=1.512 ns/op`, `take_prefetched_empty=1.709 ns/op`, 0 alloc/op), cross-check `third_party/connect-ip-go` + `hiddify-sing-box ./transport/masque/... ./protocol/masque/...` PASS.
- **[DONE] CONNECT-UDP transient drop accounting de-amplification for receive/read/send paths (vendor, resilience + hot-path stability):** в `third_party/masque-go/proxy.go` убран per-error atomic/log churn на путях transient drop вне fallback writer: `proxyConnSend` (`ReceiveDatagram`) и `proxyConnReceive` (`UDP Read` / `HTTP DATAGRAM Send`) теперь копят drop-события внутри цикла и флашат батчем (`flushTransientHTTPDatagramReceiveDrops`, `flushTransientUDPReadDrops`, `flushTransientHTTPDatagramSendDrops`) при прогрессе или выходе. Это снижает self-inflicted contention/log-pressure под transient error-storm при high PPS без изменения tolerant-drop и fail-closed контрактов для permanent ошибок. Локально: `third_party/masque-go` targeted `go test`+bench PASS (`context_zero_fast_path=1.333 ns/op`, `context_non_zero_varint=1.338 ns/op`, `extract_context_zero_fast_path=1.518 ns/op`, `take_prefetched_empty=1.707 ns/op`, 0 alloc/op), cross-check `third_party/connect-ip-go` (`policy_enabled_validate=14.25 ns/op`) и `hiddify-sing-box ./transport/masque/... ./protocol/masque/...` PASS; Win Docker smoke `udp`/`tcp_ip` (`10 MiB @ 25_000_000 B/s`) PASS (`stop_reason=none`, hash/loss drift отсутствует).
- **[DONE] CONNECT-IP empty peer-assignment fail-closed fix (vendor, correctness + resilience):** в `third_party/connect-ip-go/conn.go` устранён edge-case, где `AssignAddresses([])` включал policy-path и мог ложноположительно блокировать egress/incoming (`destination address not allowed`) вместо режима unrestricted. Пустые `peerAddresses` теперь канонизируются в `nil` (`cloneOrNilPrefixes`), `shouldValidatePolicy` проверяет `len(peerAddresses) > 0`. Добавлены регрессии в `conn_test.go`: `TestAssignAddressesEmptyKeepsPolicyBypassed` и `composeDatagram allows unrestricted egress after empty AssignAddresses`. Локально: `go test -count=1 ./...` PASS, targeted bench PASS (`policy_disabled_bypass=1.411 ns/op`, `policy_enabled_validate=13.76 ns/op`, 0 alloc/op), smoke `udp`/`tcp_ip` (`10 MiB @ 25_000_000 B/s`) PASS.
- **[DONE] CONNECT-IP same-family policy snapshot split (vendor, hot path):** в `third_party/connect-ip-go/conn.go` route/prefix policy в `connRouteView` предразделён на `IPv4/IPv6` (`peer/assigned/localRoutes`), RX/TX проверки идут по same-family срезам без per-entry `BitLen()` фильтров. Microbench `BenchmarkRoutePolicyLookups`: `same_family_only=10.37 ns/op` vs `mixed_family_scan=12.95 ns/op` (0 alloc/op).
- **[DONE] CONNECT-IP egress policy parse de-dup в `WritePacket` (vendor, hot path):** в `third_party/connect-ip-go/conn.go` убран лишний повторный `packetTuple` в пути `composeDatagram -> validateOutgoingProxiedPacket`: tuple теперь разбирается один раз и только при активной policy (`assigned/localRoutes/peer`), что уменьшает CPU на packet-plane и убирает лишний разбор IPv6 extension-chain на каждом egress пакете.
- **[DONE] CONNECT-IP ingress destination policy single-pass route decision (vendor, hot path):** в `third_party/connect-ip-go/conn.go` (`handleIncomingProxiedPacketWithView`) route validation для `dst` переведена с двойного lookup (`routesAllowDestination...` + `routeAllowsDestinationButNotProtocol...`) на единый `evaluateRouteDestinationPolicySameFamily`, который возвращает allow/reject reason за один поиск маршрута. Семантика policy-drop ICMP (`dst_not_allowed`/`proto_not_allowed`) сохранена, лишний route-search в ingress hot path убран. Локально: targeted `go test`+bench PASS (`policy_enabled_validate=13.59 ns/op`, `same_family_only=11.06 ns/op`, `binary_search_large_route_set=62.28 ns/op`, 0 alloc/op).
- **[DONE] CONNECT-IP ingress destination route-index dedupe внутри single-pass policy path (vendor, hot path):** в `third_party/connect-ip-go/conn.go` (`validateIncomingProxiedPacketWithView`) индекс маршрута `dstRouteIdx` вычисляется один раз и переиспользуется как для условия IPv6 extension-chain parse (`IPProtocol!=0`), так и для финальной route policy classification (`evaluateRouteDestinationPolicyAtIndex`). Это убирает повторный `routeContainingAddrIndex` на policy-checked ingress datagram при сохранении прежних `dst_not_allowed`/`proto_not_allowed` причин. Локально: targeted `go test`+bench PASS (`policy_enabled_validate=14.10 ns/op`, `binary_search_large_route_set=62.40 ns/op`, `take_prefetched_empty=1.697 ns/op`, 0 alloc/op); cross-check `hiddify-sing-box ./transport/masque/... ./protocol/masque/...` PASS; smoke `udp`/`tcp_ip` (`10 MiB @ 25_000_000 B/s`) PASS, hash/loss regressions не наблюдаются.
- **[DONE] CONNECT-IP ingress malformed/unknown drop accounting batching (vendor, hot path resilience):** в `third_party/connect-ip-go/conn.go` (`ReadPacketWithContext`) per-packet `atomic.Add` для tolerant-drop (`malformed`/`unknown context`) заменён на локальный batched accounting с threshold flush (`connDropCounterFlushThreshold=256`) и flush на exit/success paths. Это снижает atomic contention на drop-storm ingress под high PPS без изменения RFC tolerant-drop/deadline контрактов. Добавлен регрессионный `TestIncomingDatagrams/batched malformed and unknown drops are flushed on successful read`; локально targeted `connect-ip-go` tests+bench PASS (`policy_enabled_validate=14.06 ns/op`, `take_prefetched_empty=1.700 ns/op`, 0 alloc/op), cross-check `third_party/masque-go` и `hiddify-sing-box ./transport/masque/... ./protocol/masque/...` PASS.
- **[DONE] CONNECT-IP ingress validation-drop accounting batching (vendor, hot path resilience):** в `third_party/connect-ip-go/conn.go` (`ReadPacketWithContext`) policy/format validation reject path (`validateIncomingProxiedPacketWithView`) переведён с per-packet `atomic+sampled log` на pending batched accounting + threshold flush (`connDropCounterFlushThreshold=256`) и batched sampled flush (`batch_drop/total/last_error`). Это снижает atomic/log churn на high-PPS drop-storm ingress без изменения tolerant-drop/deadline контрактов. Добавлен `TestIncomingDatagrams/batched validation drops are flushed on successful read`; локально targeted `connect-ip-go` tests+bench PASS (`policy_enabled_validate=13.58 ns/op`, `take_prefetched_empty=1.695 ns/op`, 0 alloc/op), cross-check `third_party/masque-go` + `hiddify-sing-box ./transport/masque/... ./protocol/masque/...` PASS.
- **[DONE] CONNECT-IP/CONNECT-UDP read-loop context check fast-path (`ctxDone` cache) (vendor, hot path):** в `third_party/connect-ip-go/conn.go` (`ReadPacketWithContext`) и `third_party/masque-go/conn.go` (`proxiedConn.ReadFrom`) проверка дедлайна/отмены переведена на cached `ctx.Done()` gate: при `Done()==nil` (доминирующий steady-state без дедлайна) loop больше не делает повторные `ctx.Err()` на каждый datagram/drop, при активном `Done()` сохранена прежняя семантика deadline/cancel в тех же ветках. Локально: targeted tests+bench PASS (`connect-ip policy_enabled_validate=13.59 ns/op`, `policy_disabled_bypass=1.405 ns/op`, `take_prefetched_empty=1.701 ns/op`; `masque-go context_zero_fast_path=1.480 ns/op`, `context_non_zero_varint=1.338 ns/op`, `take_prefetched_empty=1.665 ns/op`, 0 alloc/op), `hiddify-sing-box ./transport/masque/... ./protocol/masque/...` PASS.
- **[DONE] CONNECT-UDP server ingress fast-path без drainer + no-op write elimination (vendor, hot path resilience):** в `third_party/masque-go/proxy.go` (`proxyConnSend`) добавлен отдельный fast-path для runtime без `TryReceiveDatagram` (`drainer == nil`) и убран unconditional `writePayloadBatch` на `batchCount==0` в общем цикле. Это снижает лишний bookkeeping/write churn в drop-only/no-drainer сценариях при сохранении текущего transient backoff и tolerant-drop контракта. Добавлен `TestProxyConnSendWithoutDrainerFastPath`; локально: targeted `third_party/masque-go` tests+bench PASS (`context_zero_fast_path=1.439 ns/op`, `context_non_zero_varint=1.323 ns/op`, `extract_context_zero_fast_path=1.517 ns/op`, `take_prefetched_empty=1.704 ns/op`, 0 alloc/op), cross-check `third_party/connect-ip-go` и `hiddify-sing-box ./transport/masque/... ./protocol/masque/...` PASS.
- **[DONE] CONNECT-UDP server ingress stale-backoff progress-gap fix (vendor, resilience under mixed ingress):** в `third_party/masque-go/proxy.go` (`proxyConnSend`) progress-path больше не определяется только через `batchCount>0`: добавлен marker по факту forward progress (`blocking context-0 accept` или `TryReceiveDatagram` дал forwardable payload даже если `batchCount==0` после internal batch flush). Это убирает ложное удержание/наращивание `recvBackoff` на batch-boundary и снижает риск self-throttle при mixed ingress (`valid + drop noise`) под high PPS. Добавлен `TestShouldMarkProxyConnSendProgress`; локально targeted `third_party/masque-go` tests+bench PASS (`context_zero_fast_path=1.418 ns/op`, `context_non_zero_varint=1.323 ns/op`, `extract_context_zero_fast_path=1.514 ns/op`, `take_prefetched_empty=1.711 ns/op`, 0 alloc/op), cross-check `third_party/connect-ip-go` (`policy_enabled_validate=14.67 ns/op`) и `hiddify-sing-box ./transport/masque/... ./protocol/masque/...` PASS.
- **[DONE] CONNECT-UDP server ingress progress-marker false-positive fix (vendor, resilience under transient send pressure):** в `third_party/masque-go/proxy.go` (`proxyConnSend`) `shouldMarkProxyConnSendProgress` больше не считает progress по факту `udpPayloadAccept`/`forwardable` backlog; marker теперь учитывает только реальные UDP egress write (`written > 0`). Это закрывает gap, где accepted datagram мог быть transient-drop в fallback send-path, но цикл ошибочно делал `recvBackoff.onProgress()`, поддерживая ложный no-backoff режим и усиливая `receiver_incomplete` под sustained pressure. Обновлён `conn_internal_test.go::TestShouldMarkProxyConnSendProgress` (accept/forwardable без write => no progress). Локально: targeted `third_party/masque-go` tests+bench PASS (`context_zero_fast_path=1.106 ns/op`, `context_non_zero_varint=0.9225 ns/op`, `extract_context_zero_fast_path=1.480 ns/op`, `take_prefetched_empty=1.670 ns/op`, 0 alloc/op), cross-check `third_party/connect-ip-go` tests+bench PASS (`policy_enabled_validate=14.51 ns/op`, `take_prefetched_empty=1.695 ns/op`) и `hiddify-sing-box ./transport/masque/... ./protocol/masque/...` PASS.
- **[DONE] CONNECT-UDP server ingress transient-receive false-progress fix (vendor, resilience under mixed receive/send pressure):** в `third_party/masque-go/proxy.go` (`proxyConnSend`) ветка transient ошибки `ReceiveDatagram` больше не считает progress по `forwardable>0`; `recvBackoff` теперь сбрасывается только при реальном UDP write-progress (`written>0`). Если queued context-0 datagram были `forwardable`, но целиком dropped в fallback send-path из-за transient UDP pressure, цикл больше не делает ложный `recvBackoff.onProgress()` и не уходит в self-throttle/no-backoff режим. Локально: targeted `third_party/masque-go` tests+bench PASS (`context_zero_fast_path=1.138 ns/op`, `context_non_zero_varint=0.9422 ns/op`, `extract_context_zero_fast_path=1.518 ns/op`, `take_prefetched_empty=1.750 ns/op`, 0 alloc/op), cross-check `third_party/connect-ip-go` tests+bench PASS (`policy_enabled_validate=14.56 ns/op`, `take_prefetched_empty=1.719 ns/op`) и `hiddify-sing-box ./transport/masque/... ./protocol/masque/...` PASS.
- **[DONE] CONNECT-UDP server ingress no-write backoff gap fix after successful receive path (vendor, resilience under mixed ingress pressure):** в `third_party/masque-go/proxy.go` (`proxyConnSend`) устранён gap, где цикл после успешного `ReceiveDatagram` мог не включать bounded `recvBackoff`, если были `context-id=0` accept/forwardable, но фактический UDP egress прогресс отсутствовал (`written==0` из-за transient send-pressure/drop в fallback). Добавлен единый gate `shouldBackoffProxyConnSendNoWrite(written)`; backoff теперь корректно включается на любой no-write итерации, что убирает tight read/drop loop и снижает риск `receiver_incomplete` under sustained pressure. Добавлен `TestShouldBackoffProxyConnSendNoWrite`; локально: `third_party/masque-go` targeted `go test`+bench PASS (`context_zero_fast_path=1.136 ns/op`, `context_non_zero_varint=1.121 ns/op`, `extract_context_zero_fast_path=1.513 ns/op`, `take_prefetched_empty=1.710 ns/op`, 0 alloc/op), cross-check `third_party/connect-ip-go` PASS (`policy_enabled_validate=14.56 ns/op`) и `hiddify-sing-box ./transport/masque/... ./protocol/masque/...` PASS. Win Docker smoke (`10 MiB @ 25_000_000 B/s`) после правки: `udp` PASS (`stop_reason=none`, `throughput_mbps=137.905`, `payload_hash_ok=true`, `measured_loss_pct_approx=0`), `tcp_ip` PASS (`stop_reason=none`, `hash_ok=true`, `loss_pct=0`, `throughput_mbps=37.318`, HTTP3/QUIC drop deltas=0).
- **[DONE] CONNECT-UDP server ingress split-backoff for no-write loop classes (vendor, resilience under noisy mixed ingress):** в `third_party/masque-go/proxy.go` (`proxyConnSend`) no-write итерации разделены по природе давления: send-pressure с forwardable context-0 payload остаётся на `recvBackoff`, а pure drop-only ingress (`malformed`/`unknown context`/`oversize` без forwardable) переведён на отдельный `dropOnlyBackoff` через `shouldUseDropOnlyBackoff`. Это убирает ложное накопление receive-pressure backoff на drop-шуме и снижает риск self-throttle в mixed ingress high PPS. Добавлен `TestShouldUseDropOnlyBackoff`; локально targeted `masque-go`/`connect-ip-go`/`hiddify-sing-box` тесты PASS, бенчи без alloc/regressions.
- **[DONE] CONNECT-UDP server ingress receive/send double-backoff de-dup (vendor, resilience under sustained transient send pressure):** в `third_party/masque-go/proxy.go` (`udpDatagramWriter.writePayloadBatch` + `proxyConnSend`) добавлен признак `sendPressureNoProgress`: fallback writer сообщает, что на итерации был transient send-pressure без реального UDP write-progress, и receive-loop не делает второй `recvBackoff` sleep поверх уже выполненного writer backoff. Это убирает self-inflicted double-sleep на no-write path и снижает риск throughput collapse (`receiver_incomplete`) при burst pressure без изменения tolerant-drop/fail-closed контрактов. Локально: targeted `third_party/masque-go` tests+bench PASS (`context_zero_fast_path=1.129 ns/op`, `context_non_zero_varint=0.9425 ns/op`, `extract_context_zero_fast_path=1.131 ns/op`, `take_prefetched_empty=1.698 ns/op`, 0 alloc/op), cross-check `third_party/connect-ip-go` (`policy_enabled_validate=14.50 ns/op`) и `hiddify-sing-box ./transport/masque/... ./protocol/masque/...` PASS; Win smoke в этой итерации: `tcp_ip` PASS (`throughput_mbps=36.718`, `hash_ok=true`, drop deltas=0), `udp` blocked на `transport_init/compose_up` (compose lifecycle flake).
- **[DONE] CONNECT-UDP no-drainer no-write backoff classification fix (vendor, resilience under runtimes without `TryReceiveDatagram`):** в `third_party/masque-go/proxy.go` (`proxyConnSend`, ветка `drainer == nil`) устранён gap классификации no-write итерации: добавлен helper `classifyProxyConnSendNoWriteBackoff`, который отделяет send-pressure (`sendPressureNoProgress`, sleep уже выполнен writer'ом) от drop-only ingress и не допускает лишний receive-side backoff/sleep на том же цикле. Это убирает двойной throttle и сохраняет split-backoff семантику в no-drainer runtime. Добавлен `TestClassifyProxyConnSendNoWriteBackoff`; локально: targeted `third_party/masque-go` tests+bench PASS (`context_zero_fast_path=1.129 ns/op`, `context_non_zero_varint=0.9417 ns/op`, `extract_context_zero_fast_path=1.129 ns/op`, `take_prefetched_empty=1.709 ns/op`, 0 alloc/op), cross-check `third_party/connect-ip-go` (`policy_enabled_validate=14.54 ns/op`) и `hiddify-sing-box ./transport/masque/... ./protocol/masque/...` PASS. Win Docker smoke post-check: `udp` PASS (`throughput_mbps=140.796`, `payload_hash_ok=true`, `loss≈0`), `tcp_ip` PASS (`throughput_mbps=37.184`, `hash_ok=true`, QUIC/HTTP3 drop deltas=0).
- **[DONE] CONNECT-IP ingress policy-bypass при отключённой policy (vendor, hot path):** в `third_party/connect-ip-go/conn.go` `ReadPacket` переведён на `validateIncomingProxiedPacket`: при пустом policy snapshot (`assigned/localRoutes/peer` не заданы) входящий пакет проходит без tuple parse/route scan; при активной policy поведение валидации unchanged (`handleIncomingProxiedPacket`). Microbench (`conn_test.go`): `policy_disabled_bypass=1.336 ns/op` vs `policy_enabled_validate=13.08 ns/op`, 0 alloc/op.
- **[DONE] CONNECT-IP localRoutes O(n) -> hybrid O(1)/O(log n) lookup (vendor, hot path):** в `third_party/connect-ip-go/conn.go` policy-check для `localRoutes` переведён с полного линейного скана на гибрид `linear<=8` + бинарный поиск по валидированным (`ordered + non-overlapping`) route ranges через `routeContainingAddrIndex`. Это сохраняет быстрый путь для малых route-set и резко уменьшает CPU при больших route таблицах. Bench (`conn_test.go`): `same_family_only=11.03 ns/op`, `policy_enabled_validate=13.75 ns/op`, и масштабный кейс `binary_search_large_route_set=62.13 ns/op` vs `mixed_family_scan_large_route_set=21333 ns/op`, 0 alloc/op.
- **[DONE] CONNECT-UDP datagram parse/write fast-path (vendor, hot path):** в `third_party/masque-go/conn.go` и `proxy.go` добавлен общий `parseProxiedDatagramPayload` с быстрым путём для `Context ID=0` (`0x00`) без `quicvarint.Parse` в доминирующем случае, а в `proxiedConn.WriteTo` заменены лишние `append` на прямой pre-sized write (`data[0]=0; copy(payload)`). Bench (`conn_internal_test.go`): `context_zero_fast_path=1.319 ns/op` vs `context_non_zero_varint=2.428 ns/op`, 0 alloc/op.
- **[DONE] CONNECT-IP policy route snapshot single-load (vendor, hot path):** в `third_party/connect-ip-go/conn.go` ingress `ReadPacket` и egress `composeDatagram` переведены на путь `withView` (однократный `routeView.Load()` на пакет) вместо повторных load/check по цепочке `shouldValidate* -> handle/validate*`. Это убирает лишние atomic-load и ветвления на горячем пути без изменения allow/deny семантики policy. Локальные проверки: `go test` PASS; бенчи стабильны, 0 alloc/op.
- **[DONE] CONNECT-IP/CONNECT-UDP prefetch ring modulo-elimination (vendor, hot path):** в `third_party/connect-ip-go/conn.go` и `third_party/masque-go/conn.go` индексация фиксированных prefetch-колец (`512`) переведена с `% len(ring)` на bitmask (`& 511`) в dequeue/enqueue (`takePrefetched*` / `extendPrefetchFromTry`). Это устраняет integer divide в самом горячем ingress-пути при drain DATAGRAM burst. Локальные проверки PASS; ключевые бенчи после правки: `connect-ip policy_enabled_validate=14.53 ns/op`, `policy_disabled_bypass=1.413 ns/op`; `masque-go context_zero_fast_path=1.412 ns/op` (0 alloc/op).
- **[DONE] CONNECT-IP/CONNECT-UDP malformed DATAGRAM tolerant-drop (vendor, resilience path):** в `third_party/connect-ip-go/conn.go` (`ReadPacket`) и `third_party/masque-go/conn.go`/`proxy.go` битые DATAGRAM (ошибка parse context-id/payload) переведены из fail-closed в silent drop + продолжение чтения очереди. Это убирает session reset из-за единичного битого кадра на высоких скоростях/шумах ingress. Добавлены тесты: `connect-ip TestIncomingDatagrams/malformed context datagram is dropped without closing connection`, `masque-go TestReadFromSkipsMalformedDatagrams`; полный пакет локальных MASQUE тестов PASS.
- **[DONE] CONNECT-IP/CONNECT-UDP cached try-drain + server-side bounded drain (vendor, hot path):** в `third_party/connect-ip-go/conn.go` и `third_party/masque-go/conn.go` `tryDrainHTTPDatagrams` теперь кэшируется в структуре соединения (без per-packet type assertion в `extendPrefetchFromTry`). В `third_party/masque-go/proxy.go` `proxyConnSend` добавлен bounded try-drain (`<=512`) сразу после каждого blocking `ReceiveDatagram`, что уменьшает pressure на HTTP/3 per-stream DATAGRAM queue во время burst. Локально: unit+bench PASS, 0 alloc/op в ключевых parse/policy benchmark.
- **[DONE] CONNECT-IP/CONNECT-UDP adaptive prefetch drain gating (vendor, hot path):** в `third_party/connect-ip-go/conn.go` (`ReadPacket`) и `third_party/masque-go/conn.go` (`ReadFrom`) убран безусловный вызов `extendPrefetchFromTry` после каждого успешно прочитанного datagram из уже непустого prefetch ring. Новый режим вызывает try-drain только после blocking `ReceiveDatagram` или при опустевшем локальном ring, уменьшая лишние `TryReceiveDatagram` + lock churn на high PPS без изменения wire-семантики. Локально: `go test` PASS; бенчи стабильны (`connect-ip policy_enabled_validate=14.53 ns/op`, `policy_disabled_bypass=1.404 ns/op`; `masque-go context_zero_fast_path=1.415 ns/op`), 0 alloc/op.
- **[DONE] CONNECT-UDP server-side adaptive try-drain probe backoff (vendor, hot path):** в `third_party/masque-go/proxy.go` (`proxyConnSend`) добавлен `adaptiveTryDrainGate`: после пустого `TryReceiveDatagram` probe уходит в экспоненциальный backoff (`1..64` пакетов), при появлении backlog мгновенно сбрасывается в агрессивный drain (`<=512` за цикл). Это снимает self-inflicted CPU/lock churn от пустых try-drain probe на steady-state high PPS и сохраняет burst catch-up. Добавлен `TestAdaptiveTryDrainGate`; локально `go test` PASS и bench parse path стабилен (`context_zero_fast_path=1.393 ns/op`, `context_non_zero_varint=2.372 ns/op`, 0 alloc/op).
- **[DONE] CONNECT-IP resilience under deadlines + IPv6 header-only robustness (ядро):** в vendored `third_party/connect-ip-go` добавлен `ReadPacketWithContext` и обработка контекстных дедлайнов; ядро прокидывает deadlines в `transport/masque/transport.go` через `ReadPacketWithContext` (для CONNECT-IP UDP bridge). Дополнительно, для активной policy IPv6 upper-layer protocol парсится только когда это действительно нужно (route IPProtocol!=0), чтобы не фейлить HopLimit/TTL header-only кейсы; `WritePacket` делает early closed-precheck для стабильного closed error chain. Локально: полный `go test` в `connect-ip-go` PASS и stand smoke `tcp_ip`/`udp` (10MiB @ 25_000_000) PASS (`stop_reason=none`).
- **[DONE] CONNECT-IP/CONNECT-UDP client-side adaptive prefetch probe backoff (vendor, hot path):** в `third_party/connect-ip-go/conn.go` (`adaptivePrefetchProbeGate`) и `third_party/masque-go/conn.go` (`adaptiveTryDrainGate`) `extendPrefetchFromTry` больше не делает пустой `TryReceiveDatagram` probe после каждого blocking read: включён экспоненциальный backoff (`1..64`) с мгновенным reset при обнаружении backlog. Это убирает лишний lock/probe churn на steady-state high PPS в ingress read path. Локально: `connect-ip-go` и `masque-go` unit+bench PASS, 0 alloc/op.
- **[DONE] CONNECT-IP deadline/prefetch consistency fix (vendor, resilience path):** в `third_party/connect-ip-go/conn.go` `ReadPacketWithContext` больше не проверяет `ctx.Err()` до попытки dequeue из prefetch ring; проверка дедлайна остаётся только перед blocking `ReceiveDatagram`. Это устраняет edge-case, где истёкший deadline мог отбрасывать уже полученный DATAGRAM и вносить ложные потери на boundary read path под нагрузкой. Добавлен регрессионный `TestReadPacketWithContextReturnsPrefetchedAfterDeadline`; локально `go test` + bench PASS.
- **[DONE] CONNECT-IP/CONNECT-UDP lock-free empty-prefetch fast-path (vendor, hot path):** в `third_party/connect-ip-go/conn.go` и `third_party/masque-go/conn.go` добавлен атомарный счётчик prefetch-очереди (`prefetchCountAtomic`) для доминирующего empty-case в `takePrefetchedRaw`/`takePrefetched`, чтобы не брать `prefetchMu` когда ring пуст. Это убирает лишний mutex churn в steady-state high PPS ingress path при сохранении существующей ring-семантики и bounded drain. Добавлены microbench: `BenchmarkTakePrefetchedRaw/empty_queue_fast_path=1.669 ns/op`, `BenchmarkTakePrefetched/empty_queue_fast_path=1.694 ns/op`, 0 alloc/op; целевые unit+bench пакеты PASS.
- **[DONE] CONNECT-IP/CONNECT-UDP lock-free adaptive probe-budget gate (vendor, hot path):** в `third_party/connect-ip-go/conn.go` (`adaptivePrefetchProbeGate`) и `third_party/masque-go/proxy.go` (`adaptiveTryDrainGate`, используется client+server путями) skip-budget переведён на `atomic.Int32` + CAS-decrement. Это убирает mutex/lock path для доминирующего empty-queue steady-state, когда drain probe должен быть пропущен по backoff policy. Контракт backoff/reset/cap сохранён тестами (`TestAdaptivePrefetchProbeGate`, `TestAdaptiveTryDrainGate`). Локально PASS: `connect-ip take_prefetched_empty=1.702 ns/op`, `policy_enabled_validate=13.74 ns/op`; `masque-go take_prefetched_empty=1.707 ns/op`, `context_zero_fast_path=1.415 ns/op`, 0 alloc/op.
- **[DONE] CONNECT-IP/CONNECT-UDP prefetch gate lock-elision (vendor, hot path):** в `third_party/connect-ip-go/conn.go` и `third_party/masque-go/conn.go` проверка `shouldProbe()` перенесена перед `prefetchMu.Lock()`, чтобы skip-path по backoff действительно был lock-free и не создавал mutex contention на каждом read при пустой HTTP/3 DATAGRAM очереди. Дополнительно `prefetchCountAtomic.Store(...)` в `extendPrefetchFromTry` выполняется один раз после batch drain вместо per-packet store в цикле. Локально: целевые unit+bench PASS; `connect-ip` `policy_enabled_validate=13.92 ns/op`, `take_prefetched_empty=1.691 ns/op`; `masque-go` `context_zero_fast_path=1.304 ns/op`, `take_prefetched_empty=1.668 ns/op`, 0 alloc/op.
- **[DONE] CONNECT-UDP parse fast-reject для single-byte non-zero context-id (vendor, hot path):** в `third_party/masque-go/conn.go` `parseProxiedDatagramPayload` теперь отбрасывает one-byte `context-id!=0` по префиксу QUIC varint (`00xxxxxx`) без вызова `quicvarint.Parse`; multi-byte context-id путь сохранён без изменения семантики. Цель — убрать лишний decode overhead в ingress (`proxyConnSend` и `proxiedConn.ReadFrom`) на non-zero context трафике/мусорных пакетах. Локально: `go test` PASS; `context_non_zero_varint=1.298 ns/op`, `context_non_zero_two_byte_varint=2.173 ns/op`, 0 alloc/op.
- **[DONE] CONNECT-UDP server ingress batched UDP send path (vendor, hot path):** в `third_party/masque-go/proxy.go` `proxyConnSend` переводит backlog DATAGRAM в Linux `WriteBatch` (до `16` payloads за syscall) после `ReceiveDatagram`+`TryReceiveDatagram`, с автоматическим fallback на одиночный `conn.Write` при unsupported/error path. Семантика tolerant-drop (malformed/unknown context/oversize) и RFC поведение не изменены. Локально: `third_party/masque-go` targeted unit+bench PASS, 0 alloc/op в parse/prefetch benchmark; `hiddify-sing-box` `go test ./transport/masque/... ./protocol/masque/...` PASS.
- **[DONE] CONNECT-UDP server ingress batched UDP send window tuning (vendor, hot path):** в `third_party/masque-go/proxy.go` увеличен `proxyConnUDPSendBatchMax` (`16 -> 64`) для Linux `WriteBatch/sendmmsg` в `proxyConnSend`, чтобы снизить syscall pressure на burst backlog под high PPS без изменения tolerant-drop/RFC-семантики. Локально: `third_party/masque-go` targeted unit+bench PASS (`context_zero_fast_path=1.486 ns/op`, `context_non_zero_varint=1.315 ns/op`, `take_prefetched_empty=1.666 ns/op`, 0 alloc/op); `hiddify-sing-box` `go test -count=1 ./transport/masque/... ./protocol/masque/...` PASS.
- **[DONE] CONNECT-UDP server ingress strict context-0 single-pass parse (vendor, hot path):** в `third_party/masque-go/proxy.go` server path `proxyConnSend` переведён на `extractContextZeroPayloadForUDP`: доминирующий case (`data[0]==0x00`) обрабатывается без `quicvarint.Parse`, non-zero/malformed context datagram быстро и tolerant-drop отбрасываются, oversize sampling сохранён. Цель — уменьшить parse/branch overhead на ingress packet-plane при high PPS без изменения RFC-семантики (поддержка только context-id=0). Локально: targeted `go test`/bench и `./transport/masque` + `./protocol/masque` PASS.
- **[DONE] CONNECT-IP/CONNECT-UDP deadline-safe bounded drop-storm drain (vendor, resilience under load):** в `third_party/connect-ip-go/conn.go` (`ReadPacketWithContext`) и `third_party/masque-go/conn.go` (`ReadFrom`) добавлен bounded guard (`64`) для сценария «deadline уже истёк + бесконечный поток drop-only DATAGRAM из prefetch/TryReceiveDatagram». Теперь чтение не застревает в hot-loop tolerant-drop и гарантированно завершает read timeout (`context.DeadlineExceeded` / `os.ErrDeadlineExceeded`) без session reset. Добавлены регрессионные тесты: `TestReadPacketWithContextDeadlineExceededDuringPrefetchDropStorm`, `TestReadFromDeadlineExceededDuringPrefetchDropStorm`; targeted unit/bench + `-race` PASS.
- **[DONE] CONNECT-UDP server ingress batched-send partial-write hardening (vendor, resilience under burst):** в `third_party/masque-go/proxy.go` исправлен edge-case `WriteBatch`: при `err == nil` и `sent < len(batch)` оставшаяся часть теперь обязательно отправляется через fallback (`conn.Write`) вместо молчаливой потери tail datagram'ов. Это закрывает потенциальный loss-gap на high-rate backlog pressure при частичной записи ядром/сокетом. Локально: `third_party/masque-go` targeted `go test`+bench PASS (`context_zero_fast_path=1.423 ns/op`, `take_prefetched_empty=1.702 ns/op`, 0 alloc/op), `hiddify-sing-box` `./transport/masque/... ./protocol/masque/...` PASS.
- **[DONE] CONNECT-UDP server ingress internal-drain send-pressure propagation fix (vendor, resilience under mixed pressure):** в `third_party/masque-go/proxy.go` (`proxyConnSend`) `drainIntoBatch` теперь прокидывает `sendPressureNoProgress` из internal flush (`writer.writePayloadBatch` при заполнении батча во время `TryReceiveDatagram` drain), а receive-loop агрегирует этот флаг с финальным flush через `mergeSendPressureNoProgress`. Это закрывает blind spot, где transient send-pressure внутри drain-path терялся и мог провоцировать лишний receive-side backoff/sleep на no-write итерации. Добавлен `TestMergeSendPressureNoProgress`; локально: targeted `third_party/masque-go` tests+bench PASS (`context_zero_fast_path=1.138 ns/op`, `context_non_zero_varint=0.9451 ns/op`, `extract_context_zero_fast_path=1.131 ns/op`, `take_prefetched_empty=1.704 ns/op`, 0 alloc/op), cross-check `third_party/connect-ip-go` (`policy_enabled_validate=14.67 ns/op`) и `hiddify-sing-box ./transport/masque/... ./protocol/masque/...` PASS; Win smoke (`10 MiB @ 25_000_000 B/s`) PASS для `udp`/`tcp_ip` (`stop_reason=none`, hash/loss drift отсутствует).
- **[DONE] CONNECT-UDP single-payload transient double-write elimination (vendor, resilience + syscall-pressure):** в `third_party/masque-go/proxy.go` (`udpDatagramWriter.writePayloadBatch`) для `len(payloads)==1` убран duplicate immediate write после первой transient ошибки: вместо перехода в общий fallback loop с повторной немедленной попыткой добавлен прямой path `handleSinglePayloadTransientFallback` (bounded retry/pause/drop + корректный `sendPressureNoProgress`). Для permanent ошибок — immediate return без лишнего syscall churn. Локально: targeted `third_party/masque-go` tests+bench PASS (`context_zero_fast_path=1.120 ns/op`, `context_non_zero_varint=0.9320 ns/op`, `extract_context_zero_fast_path=1.119 ns/op`, `take_prefetched_empty=1.682 ns/op`, 0 alloc/op), `hiddify-sing-box ./transport/masque/... ./protocol/masque/...` PASS.
- **[DONE] CONNECT-UDP server ingress single-pass inline context-0 extract refactor (vendor, hot path):** в `third_party/masque-go/proxy.go` `extractContextZeroPayloadForUDP` переведён на единый parse-flow с общим payload-size check после определения varint-width (`1/2/4/8`) вместо повторных payload-slice/size-check веток. Контракт не изменён: `context-id=0` accept, non-zero -> `unknown context`, truncated multi-byte -> `malformed`, oversize -> `oversize`. Локально: targeted `third_party/masque-go` tests+bench PASS (`extract_context_zero_fast_path=1.140 ns/op`, `extract_non_zero_single_byte_fast_reject=1.193 ns/op`, `extract_context_zero_two_byte=1.707 ns/op`, `context_non_zero_varint=0.9511 ns/op`, `take_prefetched_empty=1.719 ns/op`, 0 alloc/op), cross-check `third_party/connect-ip-go` tests+bench PASS, `hiddify-sing-box ./transport/masque/... ./protocol/masque/...` PASS. Win Docker smoke (`10 MiB @ 25_000_000 B/s`): `udp` PASS (`throughput_mbps=138.135`, `payload_hash_ok=true`, `stop_reason=none`), `tcp_ip` PASS (`throughput_mbps=37.045`, `hash_ok=true`, `loss_pct=0`, QUIC/HTTP3 drop deltas=0).
- **[DONE] CONNECT-UDP server ingress adaptive gate hardening для forced try-drain probes (vendor, resilience + throughput):** в `third_party/masque-go/proxy.go` (`proxyConnSend`) forced probe (`force=true`) больше не увеличивает `adaptiveTryDrainGate` skip-budget при пустом drain (`forwardable=0`); observe/backoff обновляется только для обычных probe или forced probe с реальным forward progress. Это убирает ложный рост probe backoff на drop-noise bursts и снижает риск delayed catch-up/self-throttle на mixed ingress under high PPS. Добавлен `TestShouldObserveDrainProbe`; локально targeted `masque-go` tests+bench PASS (`context_zero_fast_path=1.163 ns/op`, `context_non_zero_varint=0.965 ns/op`, `extract_context_zero_fast_path=1.576 ns/op`, `take_prefetched_empty=1.749 ns/op`, 0 alloc/op), cross-check `connect-ip-go` + `hiddify-sing-box ./transport/masque/... ./protocol/masque/...` PASS. Win Docker smoke post-check: `tcp_ip` PASS (`hash_ok=true`, `loss_pct=0`, `throughput_mbps=34.935`, drop deltas=0), `udp` blocked на `transport_init/compose_up` (compose conflict / `masque-server-core exited before healthy`).
- **[DONE] CONNECT-UDP send fallback resilience-tuning after reproducible `receiver_incomplete` (vendor, resilience + throughput):** в `third_party/masque-go/proxy.go` после стабильного воспроизведения `udp receiver_incomplete (~0.497 Mbps)` на Windows compose-stable datapath обновлён контракт fallback: `dropOnlyPressureBackoffMaxShift` уменьшен `2 -> 1` (max `100us`), `transientFallbackRetryMaxPerBatch` увеличен `1 -> 2`, `transientFallbackDropTailMinRemaining` увеличен `8 -> 32`. Цель — убрать self-throttle/no-write oscillation и снизить tail under-send/hash drift на transient send pressure без возврата busy-spin. `conn_internal_test.go` обновлён (`TestTransientBackoffDurationWithMaxShift`), targeted `masque-go` tests+bench PASS (`context_zero_fast_path=1.113 ns/op`, `context_non_zero_varint=0.9277 ns/op`, `extract_context_zero_fast_path=1.113 ns/op`, `take_prefetched_empty=1.675 ns/op`, 0 alloc/op), `connect-ip-go -race` PASS, `hiddify-sing-box ./transport/masque/... ./protocol/masque/...` PASS. Win smoke (`10 MiB @ 25_000_000 B/s`) после правки: `udp` PASS (`stop_reason=none`, `throughput_mbps=141.787`, `payload_hash_ok=true`, `loss≈0`), `tcp_ip` PASS (`hash_ok=true`, `loss_pct=0`, `throughput_mbps=36.534`, HTTP3/QUIC drop deltas=0`).
- **[IN PROGRESS] Граница CONNECT-IP на Windows Docker остаётся дисперсной после 3 повторов:** `runtime/connect_ip_udp_degrade_matrix_run1.json` → `last_pass=130m`; `run2`/`run3` → `last_pass=150m`, при этом fail плавает уже на `130m` (`budget_exceeded` / `sink_writer_boundary_no_udp_errors`). Во всех трёх повторах peer-split deltas QUIC/HTTP3 drop = **0**, UDP matrix PASS без hash drift. Следующий обязательный шаг — каноничный baseline в **WSL2**.
- **[IN PROGRESS] Блокер каноничного WSL2 replay (инфраструктура):** попытка запуска `degrade_matrix` в `Ubuntu` с `MASQUE_STAND_SKIP_GO_BUILD=1` завершилась ошибкой `docker could not be found in this WSL 2 distro` (Docker Desktop WSL integration не активна для этой distro). До снятия блокера стенд не даёт валидного post-change сигнала по `130m/140m/150m`.
- **[IN PROGRESS] Локальный Win Docker compose lifecycle остаётся нестабильным:** свежие smoke-прогоны `tcp_ip`/`udp` (10 MiB, `--udp-send-bps 25000000`) после deadline/prefetch fix снова дали `compose_up` retry/conflict (`masque-iperf-server`/`masque-server-core` name conflict, transient network recreate, ранний exit `masque-server-core`) и runtime FAIL `udp` с `receiver_incomplete`, `throughput_mbps=0.497`, `measured_loss_pct_approx=0.0549`, `payload_hash_ok=false`. До стабилизации compose lifecycle стендовый сигнал по деградации остаётся зашумлённым.
- **[IN PROGRESS] Локальный Win Docker compose lifecycle остаётся нестабильным:** свежий повтор после CONNECT-IP single-byte context fast-path сохранил ту же картину: `udp` smoke (`10 MiB`, `--udp-send-bps 25000000`) снова упал на `transport_init/compose_up` (container name conflict + `masque-server-core` exited before healthy), при этом `tcp_ip` smoke в той же сессии прошёл (`stop_reason=none`, `hash_ok=true`, `loss_pct=0`, HTTP3/QUIC drop deltas=0). До стабилизации compose lifecycle стендовый сигнал по деградации остаётся зашумлённым.
- **[IN PROGRESS] Win Docker lifecycle остаётся флаки, но свежий smoke после batched send снова зелёный:** `udp` и `tcp_ip` (10 MiB, `--udp-send-bps 25000000`) завершились PASS (`stop_reason=none`, `payload_hash_ok/hash_ok=true`, loss=0, `http3_stream_datagram_queue_drop_total=0`, `http3_datagram_unknown_stream_drop_total=0`). Нужен повторяемый WSL2 degrade replay для отделения кодового эффекта от флаков compose.
- **[IN PROGRESS] Win Docker lifecycle по-прежнему шумит после tuning batched-window (`16 -> 64`):** `udp` smoke (10 MiB, `--udp-send-bps 25000000`) снова упал на `transport_init/compose_up` (conflict/recreate loop, `masque-server-core` exited before healthy), тогда как `tcp_ip` в той же итерации прошёл (`stop_reason=none`, `hash_ok=true`, `throughput_mbps=36.605`, QUIC/HTTP3 drop deltas=0). Это подтверждает, что локальный compose-flake пока доминирует над стендовым сигналом по UDP.
- **[IN PROGRESS] Win Docker compose lifecycle всё ещё блокирует чистый стендовый сигнал после sampled policy-drop logging:** свежие smoke на `10 MiB @ 25_000_000 B/s` дали `udp` FAIL (`receiver_incomplete`, `throughput_mbps=0.497`, `measured_loss_pct_approx=0.011`, `payload_hash_ok=false`) и `tcp_ip` FAIL (`transport_init/compose_up`, conflict/recreate loop + `masque-server-core exited before healthy`). До каноничного WSL2 replay интерпретация throughput/loss как кодовой регрессии недостоверна.
- **[IN PROGRESS] После multi-byte non-zero context fast-reject свежий Win smoke снова зелёный, но без каноничного WSL replay вывод ограничен:** `udp` smoke (`10 MiB`, `--udp-send-bps 25000000`) PASS (`stop_reason=none`, `throughput_mbps=139.918`, `measured_loss_pct_approx=0`, `payload_hash_ok=true`), `tcp_ip` PASS (`hash_ok=true`, `loss_pct=0`, `throughput_mbps=37.719`, HTTP3/QUIC drop deltas=0). Нужен WSL2 `degrade_matrix` для устойчивого `last_pass/first_fail`.
- **[IN PROGRESS] После persistent fallback backoff (batch-boundary anti-spin) Win smoke остаётся неоднородным:** `udp` (`10 MiB`, `--udp-send-bps 25000000`) снова FAIL (`receiver_incomplete`, `throughput_mbps=0.497`, `payload_hash_ok=false`, timeout), при этом `tcp_ip` в той же итерации PASS (`hash_ok=true`, `loss_pct=0`, `throughput_mbps=37.379`, HTTP3/QUIC drop deltas=0). Это подтверждает, что на текущем Win Docker сигнал по UDP всё ещё нестабилен и требует каноничного WSL2 replay для оценки чистого кодового эффекта.
- **[IN PROGRESS] После ingress hot-loop allocation/log-pressure hardening Win smoke по-прежнему смешанный из-за compose lifecycle:** `udp` (`10 MiB`, `--udp-send-bps 25000000`) упал на `transport_init/compose_up` (name/network conflict + `masque-server-core exited before healthy`), в той же итерации `tcp_ip` PASS (`stop_reason=none`, `hash_ok=true`, `loss_pct=0`, `throughput_mbps=37.116`, HTTP3/QUIC drop deltas=0). Значит стендовый сигнал по UDP всё ещё зашумлён инфраструктурным флаком и требует каноничного WSL2 replay.
- **[IN PROGRESS] После threshold-batched drop flush в server ingress (`proxyConnSend`) локальный datapath сигнал mixed из-за compose lifecycle:** targeted `go test`/bench (`masque-go`, `connect-ip-go`, `transport/masque`) PASS без alloc/regression, но smoke `udp` (`10 MiB`, `--udp-send-bps 25000000`) снова упал на `transport_init/compose_up` (`masque-server-core exited before healthy`), тогда как `tcp_ip` PASS (`hash_ok=true`, `loss_pct=0`, `throughput_mbps=36.397`, HTTP3/QUIC drop deltas=0). Кодовый эффект по устойчивости drop-storm path требует каноничного WSL2 replay, иначе compose flake доминирует стендовый сигнал.
- **[IN PROGRESS] После stale-backoff progress-gap fix в `proxyConnSend` datapath на Win Docker остаётся mixed из-за compose lifecycle:** targeted `go test`/bench (`masque-go`, `connect-ip-go`, `transport/masque`) PASS без alloc/regression; smoke `udp` (`10 MiB`, `--udp-send-bps 25000000`) PASS (`stop_reason=none`, `throughput_mbps=139.104`, `payload_hash_ok=true`, `loss≈0`), но `tcp_ip` в той же итерации FAIL на `transport_init/compose_up` (conflict/recreate + `masque-server-core exited before healthy`). Для объективной оценки устойчивости fix нужен каноничный WSL2 replay без compose-flake.
- **[IN PROGRESS] После split-backoff hardening в `proxyConnSend` Win Docker сигнал снова mixed из-за compose lifecycle:** локально targeted `go test`/bench (`masque-go`, `connect-ip-go`, `transport/masque`) PASS; smoke `udp` (`10 MiB`, `--udp-send-bps 25000000`) PASS (`stop_reason=none`, `throughput_mbps=139.831`, `payload_hash_ok=true`, `loss≈0`), но `tcp_ip` в той же итерации FAIL на `transport_init/compose_up` (name/network conflict + `masque-server-core exited before healthy`). Нужен каноничный WSL2 replay, чтобы подтвердить эффект split-backoff без инфраструктурного шума.
- После **`c51e62bf`** (паритет server `QUICConfig` с packet‑plane dial + deadline в CONNECT‑IP parse‑drop loops): на Win стенде граница не стала однозначно лучше/хуже в одном сэмпле; регрессия «симметричного» серверного QUIC остаётся обязательной по архитектуре.
- **[DONE] HTTP/3 per-stream DATAGRAM try-drain → prefetch (ядро):** `stateTrackingStream.TryReceiveDatagram`; после успешного `connect-ip.Conn.ReadPacket` и `masque proxiedConn.ReadFrom` дополнительные кадры снимаются без блокировки (до **512** между вызовами приложения). Локальный prefetch — **кольцо O(1)** + без лишнего клонирования среза в CONNECT‑UDP (`hiddify-core` `f24d9f73` поверх `6ea81bfa`).
- **[DONE] CONNECT-UDP unknown-stream startup race backlog (ядро, hot path):** в `replace/quic-go-patched/http3/conn.go` увеличен bounded pending backlog (`per-stream: 64 -> 512`, `global: 4096 -> 65536`) для DATAGRAM до `TrackStream`. Быстрый стенд-факт после правки: `udp` (10 MiB, `--udp-send-bps 25000000`) перешёл из `receiver_incomplete/timeout` в **PASS** (`bytes_received=bytes_expected`, `measured_loss_pct_approx=0`, `payload_hash_ok=true`, `stop_reason=none`, `throughput_target_met=true`).
- **[DONE] TCP-over-CONNECT-IP netstack egress double-copy (ядро, hot path):** в `transport/masque/netstack_adapter.go` `writePacketWithRetry` больше не делает `bytes.Clone` + буфер повторных попыток на каждый кадр из gVisor перед `connectip.Conn.WritePacket` — compose в connect-ip-go уже копирует и мутирует TTL на внутренней копии. Снижает CPU/аллокации на исходящем пути туннеля (коммит `hiddify-core` `1cf5a685`).
- **[DONE] Smoke at ~200 Mb/s после оптимизации family-split:** `runtime/masque_python_runner_summary.json` (сценарии `udp` и `tcp_ip`, `--megabytes 10 --udp-send-bps 25000000`) показал PASS без потерь/hash drift и без новых QUIC/HTTP3 drop (`stop_reason=none`, `metrics.loss_pct=0`, peer-split deltas drop=`0`).

## 1) Зафиксированные сигналы со стенда (текущие результаты)

Условия: compose-стенд, `masque_stand_runner.py`, режим `degrade_matrix`, объём **10 MiB** на шаг, лестница TCP shaping **100 / 120 / 130 / 140 / 150 mbit/s** (`MASQUE_DEGRADE_TCP_IP_RATES`), UDP BPS синхронизированы по той же шкале. Артефакт: `experiments/router/stand/l3router/runtime/connect_ip_udp_degrade_matrix.json`.

- **CONNECT-IP (`tcp_ip_threshold`, `bulk_single_flow`) — см. актуальный `connect_ip_udp_degrade_matrix.json`:** последний записанный лестничный прогон (Docker Desktop Win, **`7d281185`**): **130m** PASS (`loss_pct=0`); **140m / 150m** FAIL с малым `loss_pct`, `stop_reason` **`sink_writer_boundary_no_udp_errors`** (раньше в истории также встречались `sink_udp_ingress_…`, `budget_exceeded` на других стендах).
  - Исторический снимок (до текущей итерации): **100 / 120 / 130** PASS; **140** — пограничный FAIL; **150** — сильнее по потерям / бюджету.

- **Большой объём (отдельный прогон):** **150 MiB** при shaping **120 mbit/s** для CONNECT-IP — **небольшие потери** (`loss_pct ≈ 0.064%`), при **100 mbit/s** на том же объёме — без потерь. То есть деградация появляется и как **скорость выше порога**, и как **длительность/объём** на граничной скорости.

Интерпретация: симптомы на стыке **ingress sink (gap)** и **runner budget** при росте скорости; при большом файле на 120 mbit/s — «долгоиграющий» сценарий с тем же классом boundary-loss, без обязательного совпадения с лестницей 10 MiB по всем точкам.

## 2) Класс 1 — QUIC / HTTP3 DATAGRAM path (самый частый по triage)

**Наблюдаемость в коде (`transport/masque/transport.go`, снапшот):**

| Счётчик | Смысл |
|--------|--------|
| `http3_stream_datagram_queue_drop_total` | per-stream очередь HTTP/3 после маршрутизации — silent drop при переполнении |
| `http3_datagram_unknown_stream_drop_total` | DATAGRAM пришёл до известного stream mapping / неверный quarter stream id path |
| `quic_datagram_rcv_queue_drop_total` | conn-level receive queue QUIC |
| `quic_datagram_packer_oversize_drop_total` | packer: кадр не влез в остаток пакета (в т.ч. HOL/after rotate) |

**Поведение в патче quic-go:**

- **Очередь send + `Rotate`:** смягчение HOL одним шагом; при узкой очереди возможен остаточный HOL глубже второй позиции; `Rotate` меняет порядок в send-queue (для ненадёжных датаграмм обычно допустимо).
- **Blocking send-queue (большой лимит):** не drop, а backpressure и рост задержки — косвенно усиливает loss на приёмной стороне при burst.
- **Per-stream queue (лимит порядка 1024 в текущей ветке):** burst без достаточного чтения приложением → прямые потери полезной нагрузки туннеля.

**Внешние треки (для стратегии апгрейда, проверять актуальность):** quic-go issues про DATAGRAM throughput/очереди (например **#3766**, **#4471**), coalescing/oversize (**#4984**), PMTU на long-lived сессиях (**#3955**).

## 3) Класс 2 — PMTU / эффективный payload / `DatagramTooLarge`

- В мосте CONNECT-IP: PTB/ICMP, `datagramCeiling`, классификация `*quic.DatagramTooLargeError` / `EMSGSIZE` как **`mtu`** — параллельно с **packer oversize** как отдельной «вселенной» счётчиков. Риск: один симптом — два разных слоя без автоматической склейки в одном `stop_reason` на стенде.
- **connect-ip-go:** нюансы ICMP / too large (апстрим issue **#38** — держать в уме при обновлении зависимости).

## 4) Класс 3 — Границы приложения (parse-drop, deadline, частичная запись)

Из `transport.go` / `endpoint_server.go` / `netstack_adapter.go`:

- **Parse-drop на IPv4/UDP без ошибки вверх** — «тихие» потери на границе моста; серверный путь отражён в `connect_ip_server_parse_drop_total` при регистрации поставщика.
- **Read deadline:** проверка не обязательно внутри длинного внутреннего цикла — снаружи может выглядеть как stall / `budget_exceeded`, а не `DeadlineExceeded`.
- **WriteTo с chunking:** при ошибке после части успешных отправок семантика «сколько реально ушло» vs возврат ошибки может путать интерпретацию loss.
- **Netstack TCP-over-CONNECT-IP:** отдельные drop при переполнении outbound queue — релевантно для TCP-over-tun, не для чистого UDP harness.

## 5) Класс 4 — Окружение Windows / WSL2 / Docker Desktop

UDP/QUIC edge cases усиливаются; **каноничный** прогон стенда — WSL + Docker Desktop (см. `AGENTS.md`). Иначе сравнение `max_pass` / `first_fail` между машинами слабо переносимо.

## 6) Процесс triage — что ещё недостаточно жёстко

- **Peer-split обязателен:** любые выводы о «где режет» — только с `delta_client` / `delta_server` и согласованными снимками счётчиков.
- **Один `stop_reason`** часто схлопывает разные причины (sink gap vs QUIC queue vs budget); нужна корреляция с §2 счётчиками и с MTU/payload полями в runtime JSON.
- **Process-wide счётчики** quic/http3 не привязаны к сессии — для узкой диагностики может понадобиться расширение (за отдельные итерации, без маскировки fail).

## 7) Зависимости — политика обновления

Обновлять **согласованным набором**: `quic-go` (патч/вендор) + `connect-ip-go` + `masque-go`, после матрицы **`max_pass + next_boundary`**, не по одному модулю.

## 8) RFC-ориентиры

- **RFC 9297** — HTTP Datagrams (семантика ненадёжности, порядок не гарантирован).
- **RFC 9484** — CONNECT-IP поверх HTTP/3.

## 9) Приоритет следующих проверок (предложение)

1. На каждом fail лестницы: полный набор §2 + PTB/PMTU поля + peer-split.
2. Отдельно прогон **большой объём** на фиксированных **100m vs 120m** для воспроизводимости «долгоиграющей» границы.
3. При росте `quic_datagram_packer_oversize_drop_total` без rcv queue drops — фокус на TX/packet budget; и наоборот.

## 10) Быстрый смоук «10 MiB при ~200 Mb/s» и ориентир ~4%

Цель: один короткий прогон вместо полной лестницы, чтобы отслеживать регрессию против исторического класса отказов (~потери порядка нескольких процентов при высоком pacing — см. triage `budget_exceeded`/sink в §1 и §6).

### Команды `masque_stand_runner.py` (байт/с pacing)

- **CONNECT-UDP** (`scenario udp`): `python masque_stand_runner.py --scenario udp --megabytes 10 --udp-send-bps 25000000` (`200e6` бит/с делить на 8 ⇒ **25_000_000 байт/с**, как в `MASQUE_UDP_RATE_BPS` в отправляющем скрипте).
- **CONNECT-IP bulk** (`scenario tcp_ip`): передать **то же** `--udp-send-bps 25000000`. На **Windows** при `--udp-send-bps 0` включается `_win_host_tcp_ip_default_udp_send_bps()` (**4_000_000 байт/с**) и оно **перебивает** расчёт из `MASQUE_TCP_IP_RATE_LIMIT`; без явного CLI выше вы не проверяете именно 200 Mb/s, даже если экспортировали переменную окружения.

### Что фиксировать в артефакте / summary

- Для `tcp_ip`: `metrics.loss_pct`, `stop_reason`, `budget_exceeded`, строки compact OBS (HTTP3/QUIC), `bytes_received`/`bytes_expected`, `hash_ok`.
- Для `udp`: `measured_loss_pct_approx`, `throughput_target_met`, `throughput_mbps`.
- **Контроль к ~4%**: не менять пороги PASS; сохранять числа и сравнивать с прошлыми прогонами (например, фиксировать `loss_pct` и факт отклонения от прошлого «якорного» fail/пограничного уровня матрицы).

### Наблюдаемость при недокачке без «near-full» (связано с §6)

Если получена умеренная потеря (~4–5%) и при этом `budget_exceeded`, то `got >= 99% expected` может **не выполняться** → `_should_collect_sink_udp_diag` не включается → `sink_udp_diag` часто остаётся `null`. Расширение сбора диагностики **только** для наблюдаемости (например, при любом `ok: false` или при `got >= 90%`) не ослабляет гейты — см. обсуждение в коде раннера.
